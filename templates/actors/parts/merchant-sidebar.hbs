<!--<aside class="mtt-merchant-sidebar">
  <nav class="mtt-merchant-nav" aria-label="{{localize 'mtt.navigation.label'}}">
    <button
      class="mtt-merchant-nav-tab {{#if (eq mtt.activeTab 'products')}}mtt-merchant-nav-tab-active{{/if}}"
      type="button"
      data-action="selectTab"
      data-tab="products"
      data-tooltip="{{localize 'mtt.tabs.products'}}"
    >
      <i class="fas fa-box"></i>
      <span>{{localize 'mtt.tabs.products'}}</span>
    </button>

    <button
      class="mtt-merchant-nav-tab {{#if (eq mtt.activeTab 'services')}}mtt-merchant-nav-tab-active{{/if}}"
      type="button"
      data-action="selectTab"
      data-tab="services"
      data-tooltip="{{localize 'mtt.tabs.services'}}"
    >
      <i class="fas fa-handshake"></i>
      <span>{{localize 'mtt.tabs.services'}}</span>
    </button>

    {{#if mtt.isEditable}}
      <button
        class="mtt-merchant-nav-tab {{#if (eq mtt.activeTab 'configuration')}}mtt-merchant-nav-tab-active{{/if}}"
        type="button"
        data-action="selectTab"
        data-tab="configuration"
        data-tooltip="{{localize 'mtt.tabs.configuration'}}"
      >
        <i class="fas fa-gear"></i>
        <span>{{localize 'mtt.tabs.configuration'}}</span>
      </button>
    {{/if}}
  </nav>
</aside>-->
